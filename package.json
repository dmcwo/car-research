{
  "name": "car-research",
  "version": "1.0.0",
  "description": "Firefox extension for car research and comparison shopping",
  "private": true,
  "scripts": {
    "bundle": "node scripts/bundle.js",
    "start": "npm run bundle && web-ext run --source-dir .",
    "build": "npm run bundle && web-ext build --source-dir . --artifacts-dir web-ext-artifacts",
    "lint": "web-ext lint --source-dir ."
  },
  "devDependencies": {
    "web-ext": "^8.10.0"
  }
}
